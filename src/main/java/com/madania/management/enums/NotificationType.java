package com.madania.management.enums;

public enum NotificationType {
    RESCHEDULE_REQUESTED,
    RESCHEDULE_APPROVED,
    RESCHEDULE_REJECTED,
    JOURNAL_COMMENT,
    LEAVE_REQUESTED,
    LEAVE_REJECTED
}
